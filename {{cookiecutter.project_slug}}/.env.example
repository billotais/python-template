# Environment variables for {{ cookiecutter.project_name }}
# Copy this file to .env and update values as needed

# Environment switcher (default, development, production, testing)
{{ cookiecutter.project_slug | upper }}_ENV=development

# Override settings via environment variables
# Prefix all variables with {{ cookiecutter.project_slug | upper }}_
# Example: {{ cookiecutter.project_slug | upper }}_DEBUG=true

{% if cookiecutter.app_type == 'api' %}
# Server settings
# {{ cookiecutter.project_slug | upper }}_HOST=0.0.0.0
# {{ cookiecutter.project_slug | upper }}_PORT=8000

# Database
# {{ cookiecutter.project_slug | upper }}_DATABASE_URL=postgresql://user:pass@localhost/db
{% endif %}
